# Stage job generators. Module names are prefixed with a two-digit stage number
# (e.g. 10_create_afterburner_jobs.py), so they are run as scripts rather than
# imported (`python simulation_pipeline/stages/10_create_afterburner_jobs.py`).
